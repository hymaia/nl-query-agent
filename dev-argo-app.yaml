apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nl-query-agent-dev
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/hymaia/nl-query-agent
    targetRevision: main
    path: helm
    helm:
      values: |
        image:
          tag: "f12efef4ec9dbb5503732573ade6435181c5e5c0"

  destination:
    server: https://kubernetes.default.svc
    namespace: agent

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
