apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nl-query-agent-dev
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/hymaia/nl-query-agent
    targetRevision: main
    path: helm
    helm:
      values: |
        image:
          tag: "5b107a0766f6347ee27c0960af7d18864a1f20c5"

  destination:
    server: https://kubernetes.default.svc
    namespace: agent

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
